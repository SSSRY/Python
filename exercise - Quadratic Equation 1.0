# '请定义一个函数quadratic(a, b, c)，接收3个参数，返回一元二次方程：ax2 + bx + c = 0 的两个解。


import math
def quadratic(a, b, c): 
    Z = (b*b) - (4*a*c)
    if Z < 0:
        raise TypeError ('invalid valid.')
    result = float (math.sqrt(Z))
    x1 = float (-b + result)/(2 * a)
    x2 = float (-b - result)/(2 * a)
    return (x1, x2)
print (radical(1, -2, 1))
print (radical(1, 6, 8))
